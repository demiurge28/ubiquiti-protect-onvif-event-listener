<Page x:Class="OnvifRecorderInstaller.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Dashboard">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource H1}"
                   Text="ONVIF Recorder"/>

        <Border Grid.Row="1" Background="#F3F4F6"
                CornerRadius="6" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="HostText" FontWeight="SemiBold"/>
                <TextBlock x:Name="VersionText" Foreground="#555"/>
                <TextBlock x:Name="ChannelText" Foreground="#555"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Check for updates" Click="Upgrade_Click"/>
            <Button Content="Re-install" Click="Reinstall_Click"/>
            <Button Content="Open admin UI" Click="OpenAdmin_Click"/>
            <Button Content="Uninstall" Click="Uninstall_Click"
                    Foreground="#B91C1C"/>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="StatusText"
                   Margin="0,16,0,0" TextWrapping="Wrap" Foreground="#555"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Change router" Click="ChangeRouter_Click"/>
            <Button Content="Close" IsDefault="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Page>
