<Page x:Class="OnvifRecorderInstaller.Pages.InstallPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Running">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="Header"
                   Style="{StaticResource H1}"
                   Text="Installing…"/>

        <ProgressBar Grid.Row="1"
                     x:Name="Progress"
                     Height="6"
                     IsIndeterminate="True"/>

        <TextBox Grid.Row="2"
                 x:Name="LogBox"
                 Margin="0,12,0,0"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel" x:Name="CancelButton"
                    Click="Cancel_Click"/>
            <Button Content="Close" x:Name="CloseButton"
                    IsEnabled="False" Click="Close_Click"/>
            <Button Content="Continue" x:Name="NextButton"
                    IsDefault="True" IsEnabled="False"
                    Click="Next_Click"/>
        </StackPanel>
    </Grid>
</Page>
